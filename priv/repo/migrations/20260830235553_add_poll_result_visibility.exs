defmodule Polly.Repo.Migrations.AddPollResultVisibility do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_sqlite.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:polls) do
      add :result_visibility, :text, null: false, default: "credentialed"
    end
  end

  def down do
    alter table(:polls) do
      remove :result_visibility
    end
  end
end
